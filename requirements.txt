# LLM client (Ollama-compatible via base_url)
openai>=1.30.0

# Web server (request catcher)
flask>=3.0.0

# Config / env
python-dotenv>=1.0.0
pydantic>=2.0.0
pydantic-settings>=2.0.0

# Analysis
pandas>=2.0.0
matplotlib>=3.8.0

# Utilities
rich>=13.0.0
loguru>=0.7.0

# Testing
pytest>=8.0.0
pytest-asyncio>=0.23.0
